function calculateReward(userAddress) {
  // ... some code to fetch user's contribution ...

  let reward = contribution * rewardRate; // rewardRate is a global variable

  // ... some other code ...
}